/** @jsx React.DOM */
var React = require('react');

var QueryInformation = React.createClass({
  displayName: 'QueryInformation',
  render: function () {
    return (
      <div>QueryInformation</div>
    );
  }
});

module.exports = QueryInformation;