/** @jsx React.DOM */
var React = require('react');

var TableInfo = React.createClass({
  displayName: 'TableInfo',
  render: function () {
    return (
      <div>TableInfo</div>
    );
  }
});

module.exports = TableInfo;