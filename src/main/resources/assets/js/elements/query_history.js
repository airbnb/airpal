/** @jsx React.DOM */
var React = require('react');

var QueryHistory = React.createClass({
  displayName: 'QueryHistory',
  render: function () {
    return (
      <div>QueryHistory</div>
    );
  }
});

module.exports = QueryHistory;