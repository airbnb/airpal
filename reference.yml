# Logging settings
logging:

  loggers:
    org.apache.shiro: INFO
    org.skife.jdbi: TRACE

  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO

# HTTP-specific options.
server:
  applicationConnectors:
    - type: http
      port: 8081
      idleTimeout: 10 seconds

  adminConnectors:
    - type: http
      port: 8082

metaStoreConfiguration:
  driverName: com.mysql.jdbc.Driver

shiro:
  iniConfigs: ["classpath:shiro_allow_all.ini"]

airpalHosts:
  - http://127.0.0.1:8082

dataSourceFactory:
  driverClass:      com.mysql.jdbc.Driver
  url:              jdbc:mysql://127.0.0.1:13306/airpal
  user:             root

s3AccessKey:  "AKIAJ7PFGMZLEOWN4B3Q"
s3SecretKey:  "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"
s3Bucket:     "airpal-project"
